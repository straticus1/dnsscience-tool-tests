# DNS Science Environment Configuration
# Generated by Ansible on {{ ansible_date_time.iso8601 }}

# Application Settings
FLASK_ENV={{ flask_env }}
FLASK_APP={{ flask_app }}
SECRET_KEY={{ secret_key }}
DEBUG={{ flask_debug }}

# Database Configuration
DB_HOST={{ db_host }}
DB_PORT={{ db_port }}
DB_NAME={{ db_name }}
DB_USER={{ db_user }}
DB_PASSWORD={{ db_password }}
DB_POOL_SIZE={{ db_pool_size }}
DB_MAX_OVERFLOW={{ db_max_overflow }}

# Redis Configuration
REDIS_HOST={{ redis_host }}
REDIS_PORT={{ redis_port }}
REDIS_DB={{ redis_db }}
REDIS_PASSWORD={{ redis_password }}

# AWS Configuration
AWS_REGION={{ aws_region }}
AWS_ACCOUNT_ID={{ aws_account_id }}
S3_BUCKET={{ s3_deployment_bucket }}
CLOUDFRONT_DISTRIBUTION_ID={{ cloudfront_distribution_id }}

# Stripe Configuration
STRIPE_SECRET_KEY={{ stripe_secret_key }}
STRIPE_PUBLISHABLE_KEY={{ stripe_publishable_key }}
STRIPE_WEBHOOK_SECRET={{ stripe_webhook_secret }}

# OpenSRS Configuration
OPENSRS_API_KEY={{ opensrs_api_key }}
OPENSRS_RESELLER_USERNAME={{ opensrs_reseller_username }}
OPENSRS_TEST_MODE=false
OPENSRS_API_URL=https://rr-n1-tor.opensrs.net:55443

# Google Maps Configuration
GOOGLE_MAPS_API_KEY={{ google_maps_api_key }}

# Email Configuration
SMTP_HOST={{ smtp_host }}
SMTP_PORT={{ smtp_port }}
SMTP_USER={{ smtp_user }}
SMTP_PASSWORD={{ smtp_password }}
SMTP_USE_TLS={{ smtp_use_tls }}
EMAIL_FROM={{ email_from }}

# Security Configuration
ALLOWED_HOSTS={{ allowed_hosts | join(',') }}
CORS_ORIGINS={{ cors_origins | join(',') }}
RATE_LIMIT_PER_MINUTE={{ rate_limit_per_minute }}
MAX_CONTENT_LENGTH={{ max_content_length }}
CSRF_ENABLED={{ csrf_protection }}
SESSION_LIFETIME={{ session_lifetime }}

# Monitoring Configuration
LOG_LEVEL={{ log_level }}
SENTRY_DSN={{ sentry_dsn }}
DATADOG_API_KEY={{ datadog_api_key }}

# Feature Flags
ENABLE_DEBUG_MODE={{ enable_debug_mode }}
ENABLE_MAINTENANCE_MODE={{ enable_maintenance_mode }}
ENABLE_RATE_LIMITING={{ enable_rate_limiting }}
ENABLE_CACHING={{ enable_caching }}
ENABLE_SSL={{ enable_ssl }}

# Application Paths
APP_DIR={{ app_dir }}
TEMPLATE_DIR={{ template_dir }}
STATIC_DIR={{ static_dir }}
LOG_DIR={{ log_dir }}
BACKUP_DIR={{ backup_dir }}