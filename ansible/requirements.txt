# Ansible Requirements for DNS Science Deployment

# Core Ansible
ansible>=8.0.0
ansible-core>=2.15.0

# AWS Integration
boto3>=1.28.0
botocore>=1.31.0
awscli>=1.29.0

# AWS SSM Plugin Requirements
# Note: The AWS SSM plugin for Ansible requires Session Manager plugin
# Install with: curl "https://s3.amazonaws.com/session-manager-downloads/plugin/latest/linux_64bit/session-manager-plugin.rpm" -o "session-manager-plugin.rpm"

# Database
psycopg2-binary>=2.9.7

# Templating and Utilities
jinja2>=3.1.2
pyyaml>=6.0
cryptography>=41.0.0
paramiko>=3.3.0

# Validation and Testing
jsonschema>=4.19.0
yamllint>=1.32.0